# import Project from './schema/project.graphql'
# import Skill from './schema/skill.graphql'

enum Experience {
  PROFICIENT
  EXPERIENCED
  FAMILIAR
}

# the schema allows the following query:
type Query {
  projects: [Project]
  project(id: String, slug: String): Project
  skill(id: String, slug: String): Skill
  skills(experience: Experience): [Skill]
}

# this schema allows the following mutation:
# type Mutation {
#   upvotePost(postId: Int!): Post
# }
